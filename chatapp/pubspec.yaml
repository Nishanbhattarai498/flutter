name: chatapp
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.6.2

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: 
  firebase_core: 
  firebase_storage: 
  image_picker: 
  random_string:
  firebase_auth: 
  google_sign_in:
  shared_preferences:
  intl:
  fluttertoast:
  provider:
  flutter_svg:
  flutter_svg_provider:
  path_provider:
  permission_handler:
  flutter_sound:
  audioplayers:
  firebase_messaging:

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - images/
